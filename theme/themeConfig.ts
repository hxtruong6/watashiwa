import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#52c41a", // Example green for "Golden Time" / Learning
  },
};

export default theme;
